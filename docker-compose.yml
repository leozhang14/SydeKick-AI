services:
  app: 
    build:
      context: app
    ports:
      - 3001:3001
    volumes:
      - ./app:/app
  pytorch_testing:
    build:
      context: pytorch_testing
    volumes:
      - ./pytorch_testing:/pytorch_testing
